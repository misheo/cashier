import 'package:flutter/material.dart';

class WebCategoriesBar extends StatelessWidget {
  const WebCategoriesBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
